WITH
	months
AS(
	SELECT
		calender_date,
		EXTRACT(month FROM calender_date) AS month_a
	FROM
		sfo_calendar
	WHERE
		available = 't'
	)
SELECT
	month_a,
	COUNT(month_a) as month_t
FROM
	months
GROUP BY 1
ORDER BY month_a ASC
LIMIT 12;

### This has the total available listings for every month. Now I just need to include a column for avg price for each month
